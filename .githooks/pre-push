#!/bin/sh
# Fast checks before a push: lint, typecheck and unit tests. The rest need PostgreSQL;
# docs/testing.md says which to run for a change. Skip once with `git push --no-verify`.
set -e
echo "pre-push: lint, typecheck and unit tests (skip once with git push --no-verify)"
make -s lint typecheck test-unit
