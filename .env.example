# DEFAULT_PASSWORD, SESSION_SECRET, and APP_ENCRYPTION_KEY are generated on
# first setup if omitted (`make setup`, `make docker-up`). The recovery password
# is printed in server logs at startup (username admin). Back up
# APP_ENCRYPTION_KEY with the database. Compose interpolates this file on the
# host; it is not mounted into the container.

# bundled = app + PostgreSQL via docker/docker-compose.yml. external = use DB_HOST below.
DB_MODE=bundled
DB_HOST=127.0.0.1
POSTGRES_PORT=5432
POSTGRES_DB=familyfi
POSTGRES_USER=familyfi
POSTGRES_PASSWORD=

# Optional. Examples: disable, require, verify-full.
# DB_SSL_MODE=require
# DB_SSL_ROOT_CERT=/path/to/ca.pem

# Published host port for `make docker-up` (container listens on 7001).
APP_PORT=7001

# Optional override for `make docker-up`.
# FAMILYFI_IMAGE=ghcr.io/nberardi/familyfi:latest

# Local UI without a UniFi console. Seeds dummy groups, devices, and an adult
# login (pat, same password as admin). Ignored when NODE_ENV=production.
# UNIFI_MOCK=1

# Spike CLI only (`make spike SPIKE_ARGS=discover`). Not read by the running app.
# UNIFI_API_KEY=
# UNIFI_BASE_URL=https://192.168.0.1/proxy/network/integration
# UNIFI_CONSOLE_ID=
# UNIFI_SITE_ID=
# UNIFI_SPIKE_MACS=
# UNIFI_SPIKE_INSTALL_ID=local
# UNIFI_TLS_INSECURE=1
# UNIFI_SPIKE_CONFIRM=1
