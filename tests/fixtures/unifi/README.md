# Sanitized UniFi fixtures

These files are **synthetic**. They follow official Network Integration API 10.4.57 shapes (pagination, MAC_ADDRESS source filters, BLOCK, IPV4_AND_IPV6) without household data.

Do not replace them with live console dumps. If a live response is needed for debugging, sanitize MACs, IPs, and names first and keep the file out of git.
