{
  "name": "FamilyFi",
  "install": "bash .cursor/install.sh",
  "start": "bash .cursor/start.sh",
  "terminals": [
    {
      "name": "next-dev",
      "command": "npx --yes pnpm@10.15.1 dev",
      "description": "Next.js dev server on http://localhost:3000. scripts/with-env.mjs waits for PostgreSQL, applies Prisma migrations, regenerates the client, then starts Next. Sign in as admin (DEFAULT_PASSWORD in .env) or pat (UNIFI_MOCK household)."
    }
  ],
  "ports": [
    {
      "name": "web",
      "port": 3000
    }
  ]
}
